#This Directory contain task for introduction to C programming language.

* 0-preprocessor: This script takes an Environmental Varibale CFILE and process it value which is a C program (Main.c) and Only compile it and stop at the preprocessor stage.

* 1-compiler: THis script also takes same environmental varibale and compile it and stop at the sompilation stage and store it ouput in a file with same name as the C program but with the .o extension (Object file.

* 2-assembler: This script calls and compile a C program but stops at the assembly stage without generating file with the .s extension.
