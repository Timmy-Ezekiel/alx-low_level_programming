#include <stdio.h>
#include <stdlib.h>

/**
	* main - Entry point
	*
	* Return: (0) Success
	*
	**/

int main(void)
{
	char ch;

			for (ch = 'Z'; ch >= 'A'; ch--)
			{
				putchar(ch);
			}
				putchar('\n');

return (0);
}
