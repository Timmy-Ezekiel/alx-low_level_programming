#include "stdio.h"

/**
  * cap-string - Changes all first letter of word to CAPS
  *
  * @str : string pointer 
  *
  * Return: str
  *
 **/

char *cap_string(char *str)
{
	int x, y;
	x = y = 0;
	while (str[x] != '\0')
	{
		if (s[x] == '\n' || '\t' ||
}
