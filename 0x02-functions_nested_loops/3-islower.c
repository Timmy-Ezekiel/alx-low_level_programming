#include "main.h"
#include <ctype.h>

/**
	* _islower - character case checker
	*
	*	@c : a character
	*
	* Return: (0) or (1) success;
	*
	**/
int _islower(int c)
{
	int x;

	x = islower(c);
	return (x);
}

